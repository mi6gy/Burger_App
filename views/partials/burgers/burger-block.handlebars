<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{sleep}}">
		{{#if devour}}EAT BURGER!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
